package com.hackbulgaria.programming51.week8;

import java.util.Scanner;

import com.hackbulgaria.programming51.week6.Labirinth;
import com.hackbulgaria.programming51.week6.MoveDown;
import com.hackbulgaria.programming51.week6.MoveLeft;
import com.hackbulgaria.programming51.week6.MoveRight;
import com.hackbulgaria.programming51.week6.MoveUp;

public class FindThePandaMain {

	public static void main(String[] args) {
		FindThePanda something = new FindThePanda();
		Scanner scanner = new Scanner(System.in);
		String direction;
		while(!something.isCompleted()) {
			System.out.println(something);
			System.out.println("Choose a direction to move: ");
			direction = scanner.next();
			
			switch(direction) {
				case "u" : something.movePerson(new PandaMoveUp()); break;
				case "d" : something.movePerson(new MoveDown()); break;
				case "l" : something.movePerson(new MoveLeft()); break;
				case "r" : something.movePerson(new MoveRight()); break;
			}
		}
		System.out.println(l);
		System.out.println("Mnogo si lud!");
	}

}
