package com.hackbulgaria.programming51.week7;

import java.util.Scanner;
import java.util.Vector;

public class FunctionCallsExtended {
	
	public static int evaluate(Vector strings, String function, int x) {
		
		for (int i = 0; i < strings.size(); i++) {
			String sth = (String) strings.elementAt(i);
			for (int j = 0; j < sth.length(); j++) {
				if (sth.charAt(j)) {
					
				}
			}
		}
		return x;
		
	}

	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);

		int lines = scanner.nextInt();
		Vector<String> strings = new Vector<>(lines);
		
		for (int i = 0; i < lines; i++) {
			String sth = scanner.nextLine();
			strings.addElement(sth);
			System.out.println(strings);
		}
		
		String fComposition = scanner.nextLine();
		int x = scanner.nextInt();

	}

}
